---
trigger: always_on
---

1. Never create new markdown files to create redundant summary or report files.
2. Always follow Object Oriented Principles by keeping the root directories clean and organized, with newely created files stored in respective folders.
3. If you notice that a file has over 250 lines of code in it, do what you can to refactor the file into smaller, modular, classes that can be reused.
4. Always follow strict type-safety practices
5. Always backup your logic with unit tests to enforce the integrity of the entire backend and all its sub or small components or connecting logic. This will be to verify nothing is broken after making changes.
